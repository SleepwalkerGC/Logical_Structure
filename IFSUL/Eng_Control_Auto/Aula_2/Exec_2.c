#include <stdio.h>

int main() {

    char letra;
    
    scanf("%c", &letra);
    //letra = getchar();
    
    //letra = getche(); 

    //letra = getch(); 
    
    printf("Letra = %c", letra);

    return 0;
}